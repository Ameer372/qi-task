const getHeads = () => {
  const tableHeads = [
    "id",
    "name",
    "description",
    "category",
    "quantity",
    "price",
    "total_price",
  ];

  return tableHeads;
};

export default getHeads;
